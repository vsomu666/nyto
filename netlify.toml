[build]
  command   = "npm install && npm run build"
  publish   = "build"

[build.environment]
  NODE_VERSION          = "20"
  NPM_CONFIG_PRODUCTION = "false"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200