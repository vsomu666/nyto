[build]
  command   = "npm ci --include=dev && npm run build"
  publish   = "build"

[build.environment]
  NPM_CONFIG_PRODUCTION = "false"
  NODE_VERSION          = "18"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200